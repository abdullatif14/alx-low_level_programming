#include <stdio.h>
#include <stdlib.h>

/**
 * Main: Enter function(x)
 * Description: print all numbers of base10
 * Return: sucess(0)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i + 0);
	}
	putchar("\n");
	return (0);
}
